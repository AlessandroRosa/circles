"addmap"="add the mobius map to the group from the input parameters"
"bomb"="delete all generators"
"capture.seeds"="capture seeds into the mask from current group configuration"
"capture.generators"="capture generators into the mask from current group configuration"
"clean"="clean input controls"
"close"="close pop-up window"
"focus"="set the focus to the pop-up window"
"full.group"="compute the full group from the input generators. That is, given A and B gens, you'll have the input generators A,a,B,b finally"
"generate.group"="generate the group in terms of Mobius Maps from the input generators"
"init"="initialize the candidate group of Mobius maps according to input parameters"
"move"="move pop-up window. Example: move LEFT|RIGHT TOP|BOTTOM"
"newmap"="insert a new mobius map"
"refresh"="display the generators list"
"render"="perform the rendering"
"tab"="switch to one of these tabs: 'input.form', 'maps.list', 'custom.vars', 'comment', 'preview'"
"type"="input mobius map through 4 params. Example#1: type 1 2 0 1. Example #2: type 1 2 0 1 add (input params and add mobius map to the candidate group)"
"var.bomb"="delete all entries in the vars list"
"var.delete"="delete a value from the current list. Example: var.delete _a"
"var.help"="return infos about vars input"
"var.list"="return the list of all registered variables and values"
"var.register"="register a variable to be used into the current group"
"var.select"="Select a value from the dropdown menu of registered vars. Example: var.select 0"